pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      run: npx eslint {staged_files}
    prettier:
      glob: "*.{ts,tsx,json,css,md,html,yml,yaml}"
      run: npx prettier --check {staged_files}
    typecheck:
      run: npx tsc --noEmit
