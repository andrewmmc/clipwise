{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["settings", "picker"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
